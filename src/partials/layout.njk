<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="color-scheme" content="dark light" />

        <title>{{ title or eleventyComputed.title }}</title>
        <meta name="description" content="{{ description }}" />

        <link rel="icon" href="/assets/favicon.svg" type="image/svg+html" />
        <link rel="alternate icon" href="/assets/favicon.png" type="image/png" />
        <link rel="mask-icon" href="/assets/favicon.svg" />

        <link rel="apple-touch-icon" href="/assets/maskable_icon_light.png" media="(prefers-color-scheme: no-preference)" />
        <link rel="apple-touch-icon" href="/assets/maskable_icon_dark.png" media="(prefers-color-scheme: dark)" />

        <link rel="manifest" href="/assets/manifest.json" />
        <meta name="theme-color" content="#000000" />

        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link href="https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,300;0,400;0,700;1,300;1,400;1,700&display=swap" rel="stylesheet" />

        <link rel="stylesheet" href="/css/styles.css" />
        <link rel="stylesheet" href="/css/navigation.css" />

        {% if css %}
            {% for item in css %}
                <link {{ item }} />
            {% endfor %}
        {% endif %}
    </head>
    <body tabindex="-1">
        <a class="visually-hidden" href="#main" id="main-skip-link">Skip to main content</a>
        <header>
            {% include "navigation.njk" %}
        </header>
        <main id="main">
            <div id="overlay"></div>
            {% block content %}
                {{ content | safe }}
            {% endblock %}
        </main>
        {% block footer %}
            {% include "footer.njk" %}
        {% endblock %}

        <script type="module">
            if ('serviceWorker' in navigator) {
                window.addEventListener('load', () => {
                    navigator.serviceWorker.register('/service-worker.js');
                });
            }
        </script>
        <script src="/js/navigation.js" type="module"></script>
        <script src="/js/navigation.legacy.js" nomodule></script>

        {% if js %}
            {% for item in js %}
                <script {{ item }}></script>
            {% endfor %}
        {% endif %}
    </body>
</html>